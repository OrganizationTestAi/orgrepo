- name: Test GitHub Access
  env:
    GH_TOKEN: ${{ secrets.ORG_GITHUB_TOKEN }}
  run: |

    echo "Authentication Status"

    gh auth status

    echo ""
    echo "Current User"

    gh api user

    echo ""
    echo "Listing My Repositories"

    gh repo list --limit 10
